{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexo-auth.fullname" . }}-postgres
  labels:
    {{- include "nexo-auth.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "nexo-auth.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: database
{{- end }}
