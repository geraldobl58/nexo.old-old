# Values for Staging environment
# Deploy automático via branch 'staging'
replicaCount: 1

image:
  repository: ghcr.io/geraldobl58/nexo-be
  tag: "staging-c2edc73a8a7615851a88f9ce8d8bfda742c2631e"
  pullPolicy: Always

imagePullSecrets:
  - name: ghcr-secret
ingress:
  enabled: true
  className: "nginx"
  annotations: {}
  hosts:
    - host: staging.api.nexo.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 256Mi
autoscaling:
  enabled: false
env:
  NODE_ENV: staging
  LOG_LEVEL: info
  SWAGGER_ENABLED: "false"
  METRICS_ENABLED: "true"
  KEYCLOAK_REALM: nexo-staging
envFrom: []
podDisruptionBudget:
  enabled: false

podAnnotations:
  app.kubernetes.io/commit: "c2edc73"
