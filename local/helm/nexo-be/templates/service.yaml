apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexo-be.fullname" . }}
  labels: {{- include "nexo-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "nexo-be.selectorLabels" . | nindent 4 }}
