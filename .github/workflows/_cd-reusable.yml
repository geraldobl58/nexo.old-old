# ============================================================================
# NEXO PLATFORM - CD Reusable Workflow
# ============================================================================
# Workflow reutilizável para deploy de qualquer serviço em qualquer ambiente.
# Padrão enterprise: Um único workflow de CD, parametrizado por ambiente.
# ============================================================================

name: _CD Reusable

on:
  workflow_call:
    inputs:
      service:
        description: "Nome do serviço"
        required: true
        type: string
      environment:
        description: "Ambiente de deploy (dev, qa, staging, prod)"
        required: true
        type: string
      image_tag:
        description: "Tag da imagem a ser deployada"
        required: true
        type: string
      helm_chart_path:
        description: "Caminho do Helm Chart"
        required: true
        type: string
      values_file:
        description: "Arquivo de values do ambiente"
        required: true
        type: string
      namespace:
        description: "Namespace Kubernetes"
        required: true
        type: string
      auto_sync:
        description: "Habilitar auto-sync no ArgoCD"
        required: false
        type: boolean
        default: false
      require_approval:
        description: "Requer aprovação manual"
        required: false
        type: boolean
        default: false
    secrets:
      DOCKERHUB_USERNAME:
        required: false
      DOCKERHUB_TOKEN:
        required: false

env:
  REGISTRY: docker.io
  IMAGE_NAME: geraldobl58/${{ inputs.service }}

jobs:
  # ============================================
  # Job 1: Validação Pré-Deploy
  # ============================================
  validate:
    name: 🔍 Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Helm
        uses: azure/setup-helm@v3
        with:
          version: "3.14.0"

      - name: Lint Helm Chart
        run: |
          helm lint ${{ inputs.helm_chart_path }} \
            --values ${{ inputs.values_file }}

      - name: Template Helm Chart
        run: |
          helm template ${{ inputs.service }}-${{ inputs.environment }} \
            ${{ inputs.helm_chart_path }} \
            --values ${{ inputs.values_file }} \
            --set image.tag=${{ inputs.image_tag }} \
            --namespace ${{ inputs.namespace }}

      - name: Validate Kubernetes manifests
        uses: instrumenta/kubeval-action@master
        with:
          files: |
            helm template ${{ inputs.service }}-${{ inputs.environment }} \
              ${{ inputs.helm_chart_path }} \
              --values ${{ inputs.values_file }}
        continue-on-error: true

  # ============================================
  # Job 2: Aprovação (Staging/Prod)
  # ============================================
  approval:
    name: ⏳ Approval
    runs-on: ubuntu-latest
    needs: validate
    if: inputs.require_approval
    environment:
      name: ${{ inputs.environment }}
      url: https://${{ inputs.service }}.${{ inputs.environment }}.nexo.io
    steps:
      - name: Approval Check
        run: |
          echo "✅ Deployment approved for ${{ inputs.service }} to ${{ inputs.environment }}"
          echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.image_tag }}"

  # ============================================
  # Job 3: Notify ArgoCD to Sync
  # ============================================
  deploy:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    needs: [validate, approval]
    if: always() && needs.validate.result == 'success' && (needs.approval.result == 'success' || needs.approval.result == 'skipped')
    environment:
      name: ${{ inputs.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Notify ArgoCD to Refresh
        env:
          ARGOCD_SERVER: ${{ vars.ARGOCD_SERVER }}
          ARGOCD_AUTH_TOKEN: ${{ vars.ARGOCD_AUTH_TOKEN }}
        run: |
          APP_NAME="${{ inputs.service }}-${{ inputs.environment }}"
          echo "🔄 Notifying ArgoCD to refresh ${APP_NAME}..."
          
          if [ -n "$ARGOCD_SERVER" ] && [ -n "$ARGOCD_AUTH_TOKEN" ]; then
            # Trigger hard refresh
            curl -s -X GET \
              "https://$ARGOCD_SERVER/api/v1/applications/${APP_NAME}?refresh=hard" \
              -H "Authorization: Bearer $ARGOCD_AUTH_TOKEN" \
              --insecure || echo "⚠️ Could not refresh, ArgoCD will sync automatically"
            
            echo "✅ ArgoCD notified! Auto-sync will deploy the new image."
          else
            echo "ℹ️ ArgoCD credentials not configured"
            echo "📋 ArgoCD will auto-sync within 60 seconds"
          fi

      - name: Summary
        run: |
          echo "🎉 Deployment initiated!"
          echo "Service: ${{ inputs.service }}"
          echo "Environment: ${{ inputs.environment }}"
          echo "Image Tag: ${{ inputs.image_tag }}"
          echo ""
          echo "📋 ArgoCD will automatically sync the new image."
          echo "🔗 Check: https://${{ vars.ARGOCD_SERVER }}/applications/${{ inputs.service }}-${{ inputs.environment }}"

  # ============================================
  # Job 4: Post-Deploy Verification
  # ============================================
  verify:
    name: ✅ Verify
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Deployment Complete
        run: |
          echo "🎉 Deployment pipeline completed!"
          echo "Service: ${{ inputs.service }}"
          echo "Environment: ${{ inputs.environment }}"
          echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.image_tag }}"
          echo ""
          echo "📋 ArgoCD auto-sync will apply the changes within 60 seconds."
