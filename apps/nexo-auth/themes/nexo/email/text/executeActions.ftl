${msg("executeActionsSubject")}

${msg("executeActionsBody", user.firstName, realmName)}

${msg("executeActionsBodyText", link, linkExpiration)}

---
© ${.now?string('yyyy')} Nexo. ${msg("emailFooterCopyright")}
